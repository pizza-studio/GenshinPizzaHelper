@testable import OPHelper_SharedAssembly
import XCTest

final class OPHelper_SharedAssemblyTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OPHelper_SharedAssembly().text, "Hello, World!")
    }
}
